# Runtime dependencies
# Lower bounds are kept at patched releases so the permitted range never
# resolves to a version with a known advisory.
fastapi>=0.136.3,<1.0
uvicorn[standard]>=0.49.0,<1.0
httpx>=0.28.1,<1.0
APScheduler>=3.11.2,<4.0
cryptography>=48.0.1
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0
